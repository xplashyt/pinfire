/**
 * Antes este archivo declaraba `window.WidgetCheckout`, el widget que
 * Wompi inyectaba en la página. Ya no se usa: el cobro se hace contra la
 * API REST desde nuestro propio formulario, así que los tipos viven junto
 * al código que los usa (lib/wompi-api.ts y lib/wompi-client.ts).
 */
export {};
